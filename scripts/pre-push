#!/bin/bash

echo "========================================"
echo " Running Go Test Suite Before Push..."
echo "========================================"

go test -v ./...

if [ $? -ne 0 ]; then
    echo "========================================"
    echo " TESTS FAILED: Push Aborted!"
    echo " Fix the failing tests before pushing."
    echo "========================================"
    exit 1
fi

echo "========================================"
echo " ALL TESTS PASSED: Proceeding with Push..."
echo "========================================"
exit 0